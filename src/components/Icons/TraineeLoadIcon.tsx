import type { FC } from "react";
import { type IconBaseProps } from "react-icons";
import { HiDatabase } from "react-icons/hi";

export const TraineeLoadIcon: FC<IconBaseProps> = (props) => (
  <HiDatabase {...props} />
);
